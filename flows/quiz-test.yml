# This is a YAML comment, which is entirely ignored.

# YAML files in Relate are preprocessed with Jinja2.
# See http://jinja.pocoo.org/docs/latest/templates/ for syntax.

# This brings in a helper function that is used to include pages from
# separate files. This is optional, but conceivably useful.
{% from "yaml-macros.yml" import indented_include %}

title: "Command Line"

# The "|" character introduces a 'block literal', which means all text indented below
# it is taken literally. The description property holds text marked up with 'Markdown'.
# See https://documen.tician.de/relate/content.html#relate-markup for more.
#
# So the '#' character below introduces a first-level headline (not a comment).
description: |

    # Command Line 

rules:
    start:
    -
        if_before: end_week 1
        if_has_role: [student, ta, instructor]
        if_has_fewer_sessions_than: 4
        may_start_new_session: True
        may_list_existing_sessions: True

    -
        may_start_new_session: False
        may_list_existing_sessions: True

    access:
    -
        permissions: [view, submit_answer, end_session]

    grade_identifier: la_quiz
    grade_aggregation_strategy: use_latest

    grading:
    -
        credit_percent: 100

groups:
-
    id: bootcamp 
    shuffle: False
    pages:

    # Pages can be given in-line (like this one) or brought in by including
    # them from other files, as below.
    -
        {{ indented_include("markdowns/00_frontmatter.yml", 8) }}
    -
        {{ indented_include("markdowns/01_first_command.yml", 8) }}
    -
        {{ indented_include("markdowns/02_the_tree.yml", 8) }}
    -
        {{ indented_include("markdowns/03_finding_yourself.yml", 8) }}
    -
        {{ indented_include("markdowns/04_making_directories.yml", 8) }}
    -
        {{ indented_include("markdowns/05_moving_around.yml", 8) }}
    -
        {{ indented_include("markdowns/06_the_root_directory.yml", 8) }}
    -
        {{ indented_include("markdowns/07_going_up.yml", 8) }}
    -
        {{ indented_include("markdowns/08_absolute_and_relative.yml", 8) }}
    -
        {{ indented_include("markdowns/09_going_home.yml", 8) }}
    -
        {{ indented_include("markdowns/10_advanced_ls.yml", 8) }}
    -
        {{ indented_include("markdowns/11_man_pages.yml", 8) }}
    -
        {{ indented_include("markdowns/12_removing_directories.yml", 8) }}
    -
        {{ indented_include("markdowns/13_tab_completion.yml", 8) }}
    -
        {{ indented_include("markdowns/14_making_files.yml", 8) }}
    -
        {{ indented_include("markdowns/15_moving_files.yml", 8) }}
    -
        {{ indented_include("markdowns/16_renaming_files.yml", 8) }}
    -
        {{ indented_include("markdowns/17_moving_directories.yml", 8) }}
    -
        {{ indented_include("markdowns/18_removing_files.yml", 8) }}
    -
        {{ indented_include("markdowns/19_copying_files.yml", 8) }}
    -
        {{ indented_include("markdowns/20_copying_directories.yml", 8) }}
    -
        {{ indented_include("markdowns/21_viewing_files_less.yml", 8) }}
    -
        {{ indented_include("markdowns/22_viewing_files_cat.yml", 8) }}
    -
        {{ indented_include("markdowns/23_counting_characters.yml", 8) }}
    -
        {{ indented_include("markdowns/24_editing_small_files.yml", 8) }}
    -
        {{ indented_include("markdowns/25_the_path.yml", 8) }}
    -
        {{ indented_include("markdowns/26_matching_lines.yml", 8) }}
    -
        {{ indented_include("markdowns/27_working_with_columns.yml", 8) }}
    -
        {{ indented_include("markdowns/28_combining_commands.yml", 8) }}
    -
        {{ indented_include("markdowns/29_power_commands.yml", 8) }}

completion_text: |

    # See you !

    Thanks for completing the quiz.
